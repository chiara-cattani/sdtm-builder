% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_dummy_study.R
\name{make_dummy_study}
\alias{make_dummy_study}
\title{Generate a complete dummy study for testing}
\usage{
make_dummy_study(
  seed = 123,
  n_subjects = 30,
  bad_case = NULL,
  starter_kit_dir = NULL
)
}
\arguments{
\item{seed}{Integer. Random seed. Default \code{123}.}

\item{n_subjects}{Integer. Number of subjects. Default \code{30}.}

\item{bad_case}{Character or \code{NULL}. Inject a specific data quality issue.
Supported values:
\itemize{
\item \code{"dup_dm_key"}: duplicate one USUBJID in DM
\item \code{"bad_ct_value"}: insert unmapped severity "TERRIBLE" in AE
\item \code{"invalid_date"}: insert "2025-13-01" in AE start date
\item \code{"missing_req_var"}: drop \code{aeterm} column from ae_raw
\item \code{NULL}: clean data (default)
}}

\item{starter_kit_dir}{Character. Path to starter kit files. Default uses
the installed package path.}
}
\value{
Named list (see Description).
}
\description{
Creates a named list containing:
\itemize{
\item \code{raw_data}: named list of tibbles (\code{dm_raw}, \code{ae_raw}, \code{cm_raw}, \code{mh_raw},
\code{pr_raw}, \code{ex_raw}, \code{vs_raw}, \code{lb_raw}, \code{ds_raw}, \code{qs_raw})
\item \code{target_meta}: tibble loaded from starter kit
\item \code{source_meta}: tibble loaded from starter kit
\item \code{ct_lib}: tibble loaded from starter kit
\item \code{config}: \code{sdtm_config} object
}

All randomness is controlled by \code{seed}.  Calling \code{make_dummy_study(seed=123)}
always produces identical output.
}
\section{Design choices}{

\itemize{
\item \strong{30 subjects} by default (SUBJ-001 to SUBJ-030)
\item \strong{Date anchor}: 2025-01-01; each subject offset by 0–60 days
\item \strong{AE}: 0–5 events per subject (Poisson λ = 2.5); ~75 total
\item \strong{CM}: 0–3 meds per subject (Poisson λ = 1.5); ~45 total
\item \strong{MH}: 0–4 history items per subject (Poisson λ = 2.0); ~60 total
\item \strong{PR}: 0–3 procedures per subject (Poisson λ = 1.5); ~45 total
}
}

