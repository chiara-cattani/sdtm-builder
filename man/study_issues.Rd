% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_study.R
\name{study_issues}
\alias{study_issues}
\title{Extract all issues (errors and warnings) from a run_study result}
\usage{
study_issues(study_result, severity = c("ERROR", "WARNING"))
}
\arguments{
\item{study_result}{The list returned by \code{run_study()}.}

\item{severity}{Character vector.
Filter to specific severity levels.
Default \code{c("ERROR", "WARNING")} — excludes NOTEs.}
}
\value{
A tibble with columns: \code{domain}, \code{severity},
\code{variable}, \code{rule_id}, \code{message}, \code{n_records},
\code{example}.
}
\description{
Convenience function to inspect all validation findings and derivation
errors from a \code{run_study()} result as a single tidy tibble.
}
\examples{
\dontrun{
out <- run_study("metadata/config.yaml")
study_issues(out)                    # all errors and warnings
study_issues(out, severity = "ERROR") # errors only
}

}
