% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_study_metadata.R
\name{validate_study_metadata}
\alias{validate_study_metadata}
\title{Validate study metadata cross-references}
\usage{
validate_study_metadata(study_meta, ct_lib = NULL, strict = FALSE)
}
\arguments{
\item{study_meta}{Named list returned by \code{\link[=read_study_metadata_excel]{read_study_metadata_excel()}}.}

\item{ct_lib}{Tibble returned by \code{\link[=read_study_ct_excel]{read_study_ct_excel()}}, or \code{NULL}.}

\item{strict}{Logical. If \code{TRUE}, errors on any failure. If \code{FALSE}
(default), issues warnings.}
}
\value{
Invisible list of validation results:
\describe{
\item{domain_check}{Tibble of domains in Variables but not in Domains sheet.}
\item{where_clause_check}{Tibble of missing WHERE_CLAUSE_IDs.}
\item{vlm_check}{Tibble of VLM_IDs in Variables with no Value Level entry.}
\item{ct_check}{Tibble of codelist_ids not found in CT library.}
\item{passed}{Logical. TRUE if all checks passed.}
}
}
\description{
Runs a suite of consistency checks across the components returned by
\code{\link[=read_study_metadata_excel]{read_study_metadata_excel()}} and \code{\link[=read_study_ct_excel]{read_study_ct_excel()}}:
\enumerate{
\item \strong{Domain consistency} — every domain in Variables exists in Domains sheet.
\item \strong{Where clause references} — every \code{WHERE_CLAUSE_ID} in Value Level
exists in Where Clauses.
\item \strong{VLM references} — every \code{VLM_ID} in Variables exists in Value Level.
\item \strong{CT references} — every \code{codelist_id} in Variables/Value Level exists
in the CT library.
\item \strong{Extensibility} — warns if a term would need to be added to a
non-extensible codelist.
}
}
