% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_e_data_access.R
\name{load_raw_datasets}
\alias{load_raw_datasets}
\title{Load raw datasets from a directory}
\usage{
load_raw_datasets(
  dir,
  pattern = "\\\\.(sas7bdat|xlsx|xls|csv|rds|rda|rdata|xpt)$",
  recursive = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dir}{Character. Path to a directory containing raw data files.}

\item{pattern}{Character. Regex pattern for file selection. Default matches
all supported extensions.}

\item{recursive}{Logical. Search sub-directories? Default \code{FALSE}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
Named list of tibbles.
}
\description{
Scans a directory for data files (\code{.sas7bdat}, \code{.xlsx}, \code{.xls}, \code{.csv},
\code{.rds}, \code{.rda}, \code{.xpt}) and loads each into a named tibble. The dataset
name is derived from the file name (without extension), lowercased.
}
\details{
Supported formats:
\itemize{
\item \strong{SAS}: \code{.sas7bdat} (via \code{\link[haven:read_sas]{haven::read_sas()}})
\item \strong{XPT}: \code{.xpt} (via \code{\link[haven:read_xpt]{haven::read_xpt()}})
\item \strong{Excel}: \code{.xlsx}, \code{.xls} (via \code{\link[readxl:read_excel]{readxl::read_excel()}})
\item \strong{CSV}: \code{.csv} (via \code{\link[readr:read_delim]{readr::read_csv()}})
\item \strong{RDS}: \code{.rds} (via \code{\link[=readRDS]{readRDS()}})
\item \strong{RDA}: \code{.rda}, \code{.rdata} (via \code{\link[=load]{load()}})
}
}
