% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_h_builders.R
\name{build_domain}
\alias{build_domain}
\title{Build a complete SDTM domain}
\usage{
build_domain(
  domain,
  target_meta,
  source_meta,
  raw_data,
  config,
  rule_set,
  dm_data = NULL,
  sv_data = NULL,
  create_supp = NULL,
  return_provenance = TRUE,
  validate = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{domain}{Character (required).}

\item{target_meta}{Tibble.}

\item{source_meta}{Tibble.}

\item{raw_data}{Named list of tibbles.}

\item{config}{\code{sdtm_config}.}

\item{rule_set}{\code{rule_set}.}

\item{dm_data}{Tibble or \code{NULL}. DM data for RFSTDTC; also accepts
a full \code{build_result} list (auto-extracts \verb{$data}).}

\item{sv_data}{Tibble or \code{NULL}.}

\item{create_supp}{Logical or \code{NULL}. Default \code{NULL}, which means the
value from \code{config$create_supp} is used (itself defaulting to \code{TRUE}).
Set \code{FALSE} to keep all variables in the main domain instead of
splitting to SUPP--.}

\item{return_provenance}{Logical. Default \code{TRUE}.}

\item{validate}{Logical. Default \code{TRUE}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Named list (build_result).
}
\description{
Main orchestrator. Loads relevant sources, applies joins, derives
variables in dependency order, finalizes, validates, and returns the
complete domain dataset plus build artifacts.
}
