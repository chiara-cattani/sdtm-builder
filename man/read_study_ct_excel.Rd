% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_b_metadata.R
\name{read_study_ct_excel}
\alias{read_study_ct_excel}
\title{Read study controlled terminology from a multi-sheet Excel workbook}
\usage{
read_study_ct_excel(path)
}
\arguments{
\item{path}{Character. Path to the \code{.xlsx} file.}
}
\value{
Tibble with columns:
\describe{
\item{codelist_id}{Codelist identifier (e.g., \code{"YN"}, \code{"SEV"}, \code{"ROUTE"}).}
\item{codelist_name}{Human-readable codelist name.}
\item{coded_value}{The CDISC submission value (term as it appears in SDTM).}
\item{input_value}{The collected/source value to match against. If
\code{DECODE} is populated, uses that; otherwise same as \code{coded_value}.}
\item{decode}{The decode value (may be \code{NA}).}
\item{case_sensitive}{Always \code{"N"} (case-insensitive matching).}
\item{term_code}{CDISC term code (e.g., \code{"C100435"}), may be \code{NA}.}
\item{is_extensible}{\code{"Yes"} or \code{"No"} — whether the codelist allows
sponsor-defined additions.}
}
}
\description{
Parses \code{Study_CT.xlsx} containing sheets:
\itemize{
\item \strong{Codelists} — codelist definitions (names, IDs, extensibility)
\item \strong{Codelists_terms} — individual terms per codelist (submission
values, decodes, term codes)
}

Each sheet uses a \code{Select} column (\code{"Y"}) to filter active rows.

The result is a flat tibble where each row = one coded term, suitable for
use as the \code{ct_lib} component of \code{\link[=new_meta_bundle]{new_meta_bundle()}}.
}
